<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
	<title>Instagram Clone</title>
	
</head>
<body>
	<a href="<%= "register/new" %>">Click here for add new user !!!</a>
	<table>	 
			<tr>
				<th>Name</th>
				<th>Action</th>
			</tr>			
		    <% @register.each do |user| %>
			<tr>
				<td><%= user.name %></td>
				<td>
					<%= link_to("Show  -",register_path(user.id),:class=>'action show')%>
					<%= link_to("Edit  -",edit_register_path(user.id),:class=>'action edit')%>
					<%= link_to("Delete  ",delete_register_path(user.id),:class=>'action delete')%>	
					
				</td>
			</tr>	

			<%end%>
	</table>

</body>
</html>