<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
	<title>Instagram Clone</title>
	
</head>

<body>
	<marquee>
      <h2 style="color:blue;">Welcome to the Demo Instagram Website <%= current_user.name %> </h2>
    </marquee>
	<a href="<%= "posts/new" %>">Click here for add new Post !!!</a>

    <center>
	<div class="card" style="width:400px">				    

		<% @posts.each do |post| %>

		    	<%#binding.pry%>
		    <div class="card-body" style="margin-top:15px;">
		    	<%if post.file.attached?%>

		    	<% if post.file.content_type.include?("png") %>

		    	    <%= image_tag url_for(post.file), style: 'height:300px;'%>	

		    	    <% else %>

		    	    <%= video_tag url_for(post.file), controls: false, type: 'video/mp4', autoplay: :autoplay, loop: :loop, muted: :muted, width: '300px' %>
		    		
		    	<%end%>
		    	<%end%>

			    <h6 class="card-title">Post Tittle -</h6>
			    <p class="card-text"><%= post.comment %></p>
			    <a href="#" class="btn btn-primary">Like</a>
			    <a href="#" class="btn btn-primary">Comment</a>
			    <a href="#" class="btn btn-primary">Share</a>
			</div>
		<%end%>
       
    </div>
</center>
	

</body>
</html>